/**
 * Do not edit directly, this file was auto-generated.
 */

/** Pure white. Light theme elevated surface. */
export const colorWhite: string;
/** Pure black. Reserved for overlays and shadows. */
export const colorBlack: string;
/** Lightest neutral. Light-theme elevated surface above neutral.100. */
export const colorNeutral50: string;
export const colorNeutral100: string;
export const colorNeutral200: string;
export const colorNeutral300: string;
export const colorNeutral400: string;
export const colorNeutral500: string;
export const colorNeutral600: string;
export const colorNeutral700: string;
export const colorNeutral800: string;
export const colorNeutral900: string;
/** Darkest neutral. Sidebar and log surfaces that must sit below the app canvas. */
export const colorNeutral950: string;
/** Lightest lilac. Light-theme tinted surface (row hover, ghost hover). */
export const colorAccent50: string;
export const colorAccent200: string;
export const colorAccent300: string;
export const colorAccent400: string;
/** Mid lilac. Closes the ramp between 400 and 600; accent.base stays the mode-aware base. */
export const colorAccent500: string;
export const colorAccent600: string;
export const colorAccent700: string;
export const colorAccent800: string;
export const colorAccent900: string;
/** Darkest lilac. Dark-theme tinted surface, the counterpart to accent.50. */
export const colorAccent950: string;
/** Base accent primitive. Airy lilac on dark (10.03:1) and deeper violet on light (6.36:1) — calibrated up from the marketing site accent, which fails AA in both themes. */
export const colorAccentBase: string;
/** Success status primitive. */
export const colorStatusSuccess: string;
/** Warning status primitive. */
export const colorStatusWarning: string;
/** Danger status primitive. */
export const colorStatusDanger: string;
/** Info status primitive. */
export const colorStatusInfo: string;
/** App canvas. Surface role, never text. */
export const semanticBackground: string;
/** Cards, panels, table rows. Surface role. */
export const semanticSurface: string;
/** Menus, popovers, dialogs. Surface role. */
export const semanticElevatedSurface: string;
/** Primary text. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticForeground: string;
/** Secondary text, labels. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticMutedForeground: string;
/** Placeholders, timestamps, hints. Large text and non-essential metadata only (>=3:1). Never body copy. */
export const semanticSubtleForeground: string;
/** Dividers and input outlines. Non-text role. */
export const semanticBorder: string;
/** Primary interactive colour: links, active state, primary button fill. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticPrimary: string;
/** Emphasis and highlight. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticAccent: string;
/** Positive state. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticSuccess: string;
/** Caution state. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticWarning: string;
/** Destructive or error state. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticDanger: string;
/** Neutral informational state. Text-eligible: passes WCAG AA 4.5:1 on background, surface and elevated-surface in both themes. */
export const semanticInfo: string;
/** Focus ring. Non-text role. >=3:1 against background. */
export const semanticFocus: string;
/** Disabled text and controls. Exempt from contrast minimums per WCAG 1.4.3. */
export const semanticDisabled: string;
/** Panel and card fill. Alias of surface, named for the component that owns it. */
export const semanticCard: string;
/** Text on card. */
export const semanticCardForeground: string;
/** Menus, dialogs, command palette. Alias of elevated-surface. */
export const semanticPopover: string;
/** Text on popover. */
export const semanticPopoverForeground: string;
/** Recessed fill one step off card: table headers, card footers, segmented tracks. Surface role. */
export const semanticMuted: string;
/** Application shell navigation column. Sits below the canvas on dark, above it on light. */
export const semanticSidebar: string;
/** Divider between sidebar and content. */
export const semanticSidebarBorder: string;
/** Emphasised line: hover outlines, switch tracks, chart gridlines, corner marks. Non-text role, >=3:1 on background, surface and elevated-surface in both themes. */
export const semanticBorderStrong: string;
/** Form control outline at rest. */
export const semanticInput: string;
/** Text and icons on a primary fill. Inverts with the fill: white on the light-theme violet, near-black on the dark-theme lilac. Passes AA on primary in both themes. */
export const semanticPrimaryForeground: string;
/** Primary fill on hover — one ramp step away from primary, in the direction that keeps primary-foreground legible. */
export const semanticPrimaryHover: string;
/** Inline and standalone links. */
export const semanticLink: string;
/** Faintest accent tint: row hover, ghost-button hover. Non-text role. */
export const semanticAccentSurface: string;
/** Accent tint one step stronger. Non-text role. */
export const semanticAccentSurfaceHover: string;
/** Selected row, active navigation item, highlighted palette result. Non-text role. */
export const semanticSelected: string;
/** Text on a selected row. */
export const semanticSelectedForeground: string;
/** Neutral button fill, range track, count badge. Non-text role. */
export const semanticSecondary: string;
/** Text on a secondary fill. */
export const semanticSecondaryForeground: string;
/** Secondary fill on hover. Non-text role. */
export const semanticSecondaryHover: string;
/** Focus ring, drag handle active state. Alias of focus, named for the property it sets. */
export const semanticRing: string;
/** Fill of a disabled control. Exempt from contrast minimums per WCAG 1.4.3. */
export const semanticDisabledSurface: string;
/** Loading placeholder fill. Non-text role. */
export const semanticSkeleton: string;
/** Scrim behind a modal layer. Non-text role. */
export const semanticOverlay: string;
/** Hatch stripe marking a region that is not data. One step off the surface it sits on — never raise it further, it is a texture, not a message. */
export const semanticHatch: string;
/** Corner registration marks. Non-text role. */
export const semanticCornerMark: string;
/** Shadow colour for the 1px contact shadow under a resting control. */
export const semanticShadowHairline: string;
/** Shadow colour for separated and floating layers. Darker on dark, where a shadow has to work harder. */
export const semanticShadowContact: string;
/** Success tinted fill for badges and callouts — the success hue at low alpha. Non-text role. */
export const semanticSuccessSurface: string;
/** Success outline for badges and callouts. Non-text role. */
export const semanticSuccessBorder: string;
/** Warning tinted fill for badges and callouts — the warning hue at low alpha. Non-text role. */
export const semanticWarningSurface: string;
/** Warning outline for badges and callouts. Non-text role. */
export const semanticWarningBorder: string;
/** Danger tinted fill for badges and callouts — the danger hue at low alpha. Non-text role. */
export const semanticDangerSurface: string;
/** Danger outline for badges and callouts. Non-text role. */
export const semanticDangerBorder: string;
/** Info tinted fill for badges and callouts — the info hue at low alpha. Non-text role. */
export const semanticInfoSurface: string;
/** Info outline for badges and callouts. Non-text role. */
export const semanticInfoBorder: string;
/** Text and icons on a danger fill. Inverts with the fill, like primary-foreground. */
export const semanticDangerForeground: string;
/** Warning on a permanently dark surface (log view, terminal). Theme-invariant on purpose: the surface does not follow color-scheme, so neither can the text. */
export const semanticWarningOnDark: string;
/** Danger on a permanently dark surface (log view, terminal). Theme-invariant on purpose: the surface does not follow color-scheme, so neither can the text. */
export const semanticDangerOnDark: string;
/** Info on a permanently dark surface (log view, terminal). Theme-invariant on purpose: the surface does not follow color-scheme, so neither can the text. */
export const semanticInfoOnDark: string;
/** Heading family. Inter, same as body; headings differentiate by size and weight. */
export const fontHeading: string;
/** Body family. Inter for interface text. */
export const fontBody: string;
/** Monospace family. Code and machine-readable values (hashes, identifiers). Numeric tables use body with tabular figures, not mono. */
export const fontMono: string;
/** Captions, timestamps, footnotes, table metadata. */
export const typeSizeMetadata: string;
/** Labels, buttons, form captions, small UI text. */
export const typeSizeLabel: string;
/** Body copy and control text; base size for code and numeric text. 14px, not 16px: a console is read at desk distance, in density. */
export const typeSizeBody: string;
/** Card and panel titles. */
export const typeSizeH3: string;
/** Section headings. */
export const typeSizeH2: string;
/** Page headings. */
export const typeSizeH1: string;
/** Display headings and stat values — the calm ceiling. */
export const typeSizeDisplay: string;
/** Regular - body, metadata, code. */
export const typeWeightRegular: number;
/** Medium - labels, display headings. */
export const typeWeightMedium: number;
/** Semibold - headings. */
export const typeWeightSemibold: number;
/** Bold - emphasis within headings. */
export const typeWeightBold: number;
/** Display and headings. */
export const typeLineHeightTight: number;
/** Body, labels, code. */
export const typeLineHeightNormal: number;
/** Long-form reading, notes. 1.6 is rationalized - 1.75 is part of the ad-hoc set and is deliberately not reused. */
export const typeLineHeightRelaxed: number;
/** Section and card headings — between tight and normal. */
export const typeLineHeightSnug: number;
/** Display and headings - a hair tighter than body. */
export const typeLetterSpacingHeading: string;
/** Neutral tracking - the default for body, metadata and code roles. */
export const typeLetterSpacingNormal: string;
/** Labels and small UI text. */
export const typeLetterSpacingLabel: string;
/** Display headings and stat values — tighter still than heading. */
export const typeLetterSpacingDisplay: string;
/** Uppercase eyebrow labels, which need tracking opened up to stay readable. */
export const typeLetterSpacingCaps: string;
/** Display headings. */
export const typeRoleDisplay: string;
/** Page headings (h1); h2 and h3 step down through type.size. */
export const typeRoleHeading: string;
/** Body copy. */
export const typeRoleBody: string;
/** Labels, buttons, form captions. */
export const typeRoleLabel: string;
/** Captions, timestamps, footnotes. */
export const typeRoleMetadata: string;
/** Code and data values (monospace). */
export const typeRoleCode: string;
/** Numeric and tabular data. Carries the namespaced com.momoi-labs.kiso.fontVariantNumeric extension (tabular-nums), emitted by the formatter as the global --font-variant-numeric feature token. */
export const typeRoleNumeric: string;
/** Hairline gaps: segmented-control padding, stacked label/value pairs. */
export const spacing2xs: string;
/** Tight internal padding, icon gaps. */
export const spacingXs: string;
/** Component padding, small gaps. */
export const spacingSm: string;
/** Default padding, form gaps. */
export const spacingMd: string;
/** Card padding, section gaps. */
export const spacingLg: string;
/** Page margins, panel gaps. */
export const spacingXl: string;
/** Wide page margins, dialog padding. */
export const spacing2xl: string;
/** Section spacing. */
export const spacing3xl: string;
/** Large section spacing, hero padding. */
export const spacing4xl: string;
/** Checkboxes, chart bars, the smallest controls. */
export const radiusXs: string;
/** Extra-small buttons, focus-ring rounding. */
export const radiusSm: string;
/** Buttons, inputs, menu items. The control default. */
export const radiusMd: string;
/** Segmented-control tracks and other control groups. */
export const radiusLg: string;
/** Pills, dots, switches, avatars. */
export const radiusFull: string;
/** Panels, cards, tables, dialogs. Square, always — corner marks read as registration marks only against a square frame. */
export const radiusSurface: string;
/** Resting controls: buttons, inputs, selected segments. A contact line, not a shadow. */
export const shadowXs: string;
/** Cards and panels on the canvas — subtle separation. */
export const shadowSm: string;
/** Popovers and dropdowns. */
export const shadowMd: string;
/** Dialogs, drawers and the command palette — the only layers that float free. */
export const shadowLg: string;
/** Focus and hover feedback. */
export const motionDurationFast: string;
/** Open/close and state changes. */
export const motionDurationNormal: string;
/** Ease-out - elements settle, no bounce, no delight for its own sake. */
export const motionEasingStandard: string;
/** Small tablets, portrait. */
export const breakpointSm: string;
/** Tablets, landscape phones. */
export const breakpointMd: string;
/** Desktop. */
export const breakpointLg: string;
/** Wide desktop. */
export const breakpointXl: string;
/** Inline table-row actions, dense toolbars. */
export const sizeControlXs: string;
/** Toolbars, segmented controls, compact forms. */
export const sizeControlSm: string;
/** The default. Buttons, inputs, selects. */
export const sizeControlMd: string;
/** Primary calls to action, table row height. */
export const sizeControlLg: string;
/** WCAG 2.2 target-size minimum. Apply only inside @media (pointer: coarse) — as a min-height on top of a control size, never as the control size itself. */
export const sizeTouchMin: string;
/** Icons inside labels, badges and xs controls. */
export const sizeIconSm: string;
/** The default. Icons in buttons, inputs, navigation. */
export const sizeIconMd: string;
/** Empty-state and page-header icons. */
export const sizeIconLg: string;
/** Application shell navigation column width. */
export const sizeSidebar: string;
/** Maximum content measure. Beyond this a row is scanned, not read. */
export const sizeContentMax: string;
/** Marks on (1) or off (0). Opacity, so it is a single switch on the pseudo-element rather than a rule per component. */
export const cornerMark: number;
/** Length of one tick. */
export const cornerMarkTick: string;
/** Distance from tick end to the frame it points at. The gap is the mark — close it and this is just a thicker border. */
export const cornerMarkGap: string;
/** Stripe width. */
export const hatchLine: string;
/** Stripe-to-stripe distance. Half of it is stripe, half is gap. */
export const hatchPeriod: string;
/** Stripe angle. DTCG has no angle type, so the CSS unit rides in the namespaced extension and the formatter emits "45deg". */
export const hatchAngle: string;
