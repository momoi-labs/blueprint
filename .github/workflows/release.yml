name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      confirm_publish:
        description: "Type publish-kiso to publish the prepared version"
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  pull-requests: write

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  version:
    name: Prepare Kiso release
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org
      - run: npm ci
      - run: npm run check
      - uses: changesets/action@v1
        with:
          version: npx changeset version
          title: "chore: release kiso"
          commit: "chore: release kiso"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish:
    name: Publish Kiso
    if: github.event_name == 'workflow_dispatch' && inputs.confirm_publish == 'publish-kiso'
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org
      - run: npm ci
      - run: npm run check
      - run: npm run release
