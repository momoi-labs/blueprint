{
  "name": "blueprint",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "kiso/blocks/react-prototype"
  ],
  "scripts": {
    "gallery": "npm run build:packages && npm run dev --workspace=kiso-gallery",
    "build:gallery": "npm run build:packages && npm run build --workspace=kiso-gallery",
    "preview:gallery": "npm run preview --workspace=kiso-gallery",
    "prototype": "npm run build:packages && npm run dev --workspace=kiso-react-prototype",
    "build": "style-dictionary build --config style-dictionary.config.mjs",
    "check": "npm run build && npm run check:generated && npm run check:tokens",
    "check:generated": "git diff --exit-code -- tokens/build/",
    "check:tokens": "node scripts/validate-dtcg.mjs && node scripts/check-contrast.mjs tokens/tokens.json && node scripts/check-component-token-refs.mjs",
    "changeset": "changeset",
    "release": "npm run build:packages && changeset publish",
    "build:packages": "npm run build --workspace=@momoi-labs/kiso && npm run build --workspace=@momoi-labs/kiso-react",
    "build:demo": "npm run build:packages && npm run build --workspace=kiso-react-prototype",
    "check:react": "npm run build:demo && node scripts/check-react-package.mjs",
    "version:packages": "changeset version && npm install --package-lock-only"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "ajv": "^8.17.1",
    "style-dictionary": "^5.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
